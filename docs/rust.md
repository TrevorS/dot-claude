# Rust

- I prefer to use `rustup` to manage Rust versions.
- I prefer to use `cargo` for everything else (`cargo add`, `cargo run`, etc...)
- We always use `cargo clippy` to ensure our code is clean and follows best practices.
- We always use `cargo fmt` to format our code according to the Rust style guide.
