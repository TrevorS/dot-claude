[project]
name = "dot-claude"
version = "0.1.0"
description = "Claude Code configuration — skills, commands, agents, hooks, rules, and dotfiles"
requires-python = ">=3.13"
dependencies = [
    "anthropic>=0.85.0",
    "claude-agent-sdk>=0.1.49; sys_platform == 'darwin'",
    "pre-commit>=4.2.0",
]

[tool.uv]
package = false
required-environments = [
    "sys_platform == 'linux' and platform_machine == 'x86_64'",
    "sys_platform == 'darwin' and platform_machine == 'arm64'",
]

[dependency-groups]
dev = [
    "ty>=0.0.22",
]

[tool.ty]
# Scripts use inline uv script metadata for their deps (not installed in project venv)
[tool.ty.rules]
unresolved-import = "ignore"
