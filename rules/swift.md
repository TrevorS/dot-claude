---
paths:
  - "**/*.swift"
---

# Swift / SwiftUI

**ALWAYS load the `swiftui-engineer` skill** before working with SwiftUI code.

The skill covers modern SwiftUI patterns, anti-patterns to avoid, and macOS-specific guidance.
