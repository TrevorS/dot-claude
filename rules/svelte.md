---
paths:
  - "**/*.svelte"
---

# Svelte

**ALWAYS load the `svelte5` skill** before writing or modifying Svelte components.

Svelte 5 uses runes (`$state`, `$derived`, `$effect`, `$props`) instead of Svelte 4 patterns.
Training data is heavily Svelte 4 — the skill prevents outdated syntax.
