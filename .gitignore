# Local Claude settings
*.local.md
settings.local.json

# Environment files
.env
.env.*
!.env.example

# Team plan
policy-limits.json

# Logs
*.log

# Temporary files
.tmp/
*.tmp

# OS specific files
.DS_Store
Thumbs.db

# Python / build
__pycache__/
.pytest_cache/
.venv/

# Claude Code runtime
cache/
chrome/
claude_files_*/
debug/
downloads/
file-history/
ide/
image-cache/
mcp-needs-auth-cache.json
gh-pr-status-cache.json
.last-update-result.json
node_modules/
remote-settings.json
paste-cache/
plans/
scheduled_tasks.lock
plugins/
projects/
session-env/
sessions/
shell-snapshots/
stats-cache.json
statsig/
tasks/
teams/
todos/
backups/
telemetry/
certs/
daemon/
jobs/
.last-cleanup

# Emacs runtime state — elpaca packages and savefiles land next to init.el,
# and `stow --adopt` without --no-folding can fold ~/.config/emacs into the repo
dotfiles/emacs/.config/emacs/*
!dotfiles/emacs/.config/emacs/init.el
!dotfiles/emacs/.config/emacs/early-init.el

# ML models
models/

# Channel runtime state
channels/dev/
channels/house/
channels/telegram/inbox/
channels/telegram/.last-context-restore

# Channel secrets
channels/telegram/.env
channels/telegram/access.json

# Data files
*.db
*.db-shm
*.db-wal
*.jsonl
.credentials.json
channels/
daemon/
jobs/
daemon.*
daemon-*
