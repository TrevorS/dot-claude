# Cargo packages (installed via cargo binstall, falls back to cargo install).
# Upgraded via `cargo install-update` (see make upgrade).
# One package per line. Comments and blank lines are ignored.
atuin
bat
cargo-binstall
cargo-update
git-delta
jj-cli
stylua
tree-sitter-cli
